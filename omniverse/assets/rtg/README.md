# RTG asset

`RTG_Model.usdc` is generated from `../../../blender/RTG_Model.blend`.

The first export is intentionally static and is used to validate visual fidelity and stage performance. Animation, physics, collision, and live-control schemas should be added only after the hierarchy has been simplified and the moving assemblies have been separated from the static port environment.
